type User @entity {
  id: ID!
  reservations: [Reservation!]!
  referrals: [Referral!]!
  referredEth: BigInt!
}

type Reservation @entity {
  id: ID!
  user: User!
  investmentDay: BigInt!
  amount: BigInt!
}

type Referral @entity {
  id: ID!
  referrer: User!
  referee: User!
  amount: BigInt!
}